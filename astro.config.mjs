import { defineConfig } from 'astro/config';

export default defineConfig({
  // Update these with your GitHub username and repository name when deploying
  // site: 'https://USERNAME.github.io',
  // base: '/REPO_NAME',
});
